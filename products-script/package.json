{
  "name": "google-storage-sync",
  "type": "module",
  "version": "1.0.0",
  "description": "Sync local folder to Google Cloud Storage",
  "main": "gcs-sync.ts",
  "scripts": {
    "build": "ts-node build-functions.ts",
    "sync": "ts-node gcs-sync.ts",
    "copy-config": "ts-node copy-config.ts",
    "sync-all": "ts-node sync-all.ts"
  },
  "dependencies": {
    "@google-cloud/storage": "^7.18.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  },
  "private": true
}
